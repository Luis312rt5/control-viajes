import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.controlviajes.app',
  appName: 'Control de Viajes',
  webDir: 'dist'
};

export default config;
